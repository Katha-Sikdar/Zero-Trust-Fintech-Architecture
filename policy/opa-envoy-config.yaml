# OPA-Envoy plugin configuration. Exposes the decision at envoy/authz/allow.
plugins:
  envoy_ext_authz_grpc:
    addr: :9191
    path: envoy/authz/decision
    dry-run: false
    enable-reflection: true
decision_logs:
  console: true
